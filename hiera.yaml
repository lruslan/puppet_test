---
:hierarchy:
 - roles/%{role}/%{spil_environment}
 - roles/%{role}
 - environments/%{spil_environment}
 - platforms/%{platform}
 - common
 - users
:logger: console
:backends:
  - yaml
:merge_behavior: deeper
:yaml:
  :datadir: '/etc/puppet/hieradata'
